server {
        listen 8080;
        server_name dev.h0su.online;

        access_log /var/log/nginx/dev.h0su.online.access.log;
        error_log /var/log/nginx/dev.h0su.online.error.log;

    location / {
      proxy_pass http://host.containers.internal:3001;
      proxy_set_header Host $http_host;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection upgrade;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
	  proxy_http_version 1.1;
    }

}

server {
        listen 8080;
        server_name server.h0su.online;

        access_log /var/log/nginx/server.h0su.online.access.log;
        error_log /var/log/nginx/server.h0su.online.error.log;

    location / {
      proxy_pass http://host.containers.internal:3002;
      proxy_set_header Host $http_host;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection upgrade;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
	  proxy_http_version 1.1;
    }

}

